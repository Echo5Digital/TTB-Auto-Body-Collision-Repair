import type { Metadata } from "next";
import "./globals.css";

export const metadata: Metadata = {
  title: "TTB Auto Body & Collision Repair",
  description: "Generated by AI Studio",
};

// Sitewide JSON-LD (Organization/LocalBusiness + WebSite). Injected as a literal
// object by project-writer.ts from verified facts only — never from AI output —
// so this can never contain a fabricated address, phone, or credential.
const SITEWIDE_SCHEMA = [{"@context":"https://schema.org","@type":"Organization","name":"TTB Auto Body & Collision Repair","url":"https://example.com","areaServed":"Houston, Texas"},{"@context":"https://schema.org","@type":"WebSite","name":"TTB Auto Body & Collision Repair","url":"https://example.com"}];

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className="bg-white text-gray-900 antialiased">
        {SITEWIDE_SCHEMA.map((schema: Record<string, unknown>, i: number) => (
          <script
            key={i}
            type="application/ld+json"
            dangerouslySetInnerHTML={{ __html: JSON.stringify(schema) }}
          />
        ))}
        {children}
      </body>
    </html>
  );
}
